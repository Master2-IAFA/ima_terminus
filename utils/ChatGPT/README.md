In order for the usage of ChatGPT, you need to create a file named "config.py" in the same directory as this file.

The file should contain the following variable:

```python
API_KEY = "your_OpenAI_API_key"
```
